#include<stdio.h>
#include<string.h>

/**
 * main - Entry point
 * Return: 0 if output is correct
 */

int main(void)
{
	char str1[] = "\"Programming is like building a multilingual puzzle";

	puts(str1);

	return (0);
}
