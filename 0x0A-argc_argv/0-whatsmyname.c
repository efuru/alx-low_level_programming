#include <stdio.h>

/**
 * main - this function writes itself
 * @argc: argument count
 * @argv: array of strings inside the arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
