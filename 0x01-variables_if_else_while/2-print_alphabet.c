#include <stdio.h>

/**
 * main - This program that prints the alphabet in lowercase.
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char e;

	for (e = 'a'; e <= 'z'; e++)
	{
		putchar(e);
	}

	putchar('\n');

	return (0);
}
