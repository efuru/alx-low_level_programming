#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string to stdout
 * @s: pointer to the string to print
 */
void _puts(char *s)
{
	(void)s;
}
/**
 * main - main entry
 * @n: integer value
 * Return: 0
 */

int main(void)
{
	int n = 0;
	return(0);
}
